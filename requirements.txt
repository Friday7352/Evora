# Keep the transcription service on Python 3.11.  The installer enforces
# that interpreter version: it has broad Windows wheel support for the GPU
# and speaker-labelling dependencies, unlike the Python 3.14 setup this
# project previously assumed.
faster-whisper==1.2.1
Flask==3.1.3
waitress==3.0.2

# CUDA 12 libraries required by CTranslate2 on NVIDIA GPUs.
nvidia-cublas-cu12==12.6.4.1
nvidia-cudnn-cu12==9.5.1.17

# Optional speaker labelling for Evora.
numpy==2.2.6
torch==2.7.1
torchaudio==2.7.1
speechbrain==1.1.0
